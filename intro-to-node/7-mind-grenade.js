const num1 = 5
const num2 = 10

function addValues() {
    console.log(`The sum is: ${num1 + num2}`)
}

addValues() //if we have a function such as this, we don't have to assign a variable to the require global.